<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".ui.reports.reminders.RemindersActivity"
    tools:showIn="@layout/activity_reminders">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/content_reminders_rv_motorcycles"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="top"
            android:clipToPadding="false"
            android:paddingBottom="@dimen/content_reminders_rv_padding_bottom"
            android:paddingTop="@dimen/content_reminders_rv_top_padding" />

        <!-- Empty view for the list -->
        <RelativeLayout
            android:id="@+id/content_reminders_rv_empty_view"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:gravity="center"
            android:paddingBottom="?attr/actionBarSize"
            android:visibility="gone">

            <ImageView
                android:id="@+id/content_reminders_rv_empty_view_image"
                android:layout_width="@dimen/content_reminders_empty_view_image_width"
                android:layout_height="@dimen/content_reminders_empty_view_image_height"
                android:layout_centerHorizontal="true"
                android:contentDescription="@string/content_reminders_rv_empty_view_image_desc"
                app:srcCompat="@drawable/ic_brand_full" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_below="@+id/content_reminders_rv_empty_view_image"
                android:gravity="center"
                android:orientation="horizontal"
                tools:ignore="UseCompoundDrawables">

                <ImageView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:contentDescription="@string/content_reminders_rv_empty_text"
                    app:srcCompat="@drawable/ic_warning_accent" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="@dimen/content_reminders_empty_textview_text_margin_left"
                    android:text="@string/content_reminders_rv_empty_text"
                    android:textAppearance="@style/TextAppearance.AppCompat.Small"
                    android:textStyle="bold" />
            </LinearLayout>
        </RelativeLayout>
    </FrameLayout>
</androidx.core.widget.NestedScrollView>